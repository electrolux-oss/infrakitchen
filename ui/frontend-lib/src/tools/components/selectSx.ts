/** Lays out the selected option of a select like its menu item, with the icon before the text. */
export const SELECTED_ICON_SX = {
  "& .MuiSelect-select": { display: "flex", alignItems: "center", gap: 1 },
} as const;
